<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/grey_background"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.tuankhai.travelassistants.activity.SchedulePlaceActivity"
    tools:showIn="@layout/activity_schedule_place">

    <LinearLayout
        android:id="@+id/empty"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:paddingBottom="@dimen/params_250dp"
        android:visibility="gone">

        <ImageView
            android:layout_width="@dimen/width_logo"
            android:layout_height="@dimen/height_logo"
            android:layout_gravity="center"
            android:layout_marginBottom="@dimen/params_10dp"
            android:layout_marginRight="@dimen/params_20dp"
            android:scaleType="centerInside"
            app:srcCompat="@drawable/logo_small" />

        <TextView
            android:id="@+id/txt_notify"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Bạn chưa có lịch trình"
            android:textAllCaps="false"
            android:textColor="@color/global_black"
            android:textSize="@dimen/size_18sp" />
    </LinearLayout>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/lv_schedule_place"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />


</FrameLayout>
